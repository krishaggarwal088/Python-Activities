# Python program to calculate the sum of the first sixty seven natural numbers
# Use built-in sum and range functions to calculate the sum
total_sum = sum(range(1, 67))
print(f"The sum of the first sixty six natural numbers is {total_sum}")